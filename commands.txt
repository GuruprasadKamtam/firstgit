
GIT - Git is the version control system is a tool that helps to track changes in code.
It is popular, free and open source , fast and scalable.


SETUP - 
git --version - to check the version of the git.
git config --global user.name "[firstname lastname]"
git config --global user.email "[email]"
git config --list - to get the user name and email.
git config --global color.ui auto

git init - to initialize the git repo.
(git add .) - which is used to add multiple files in a git hub. it is in the staging area.
git status
git commit -m "firstcommit"
git push origin main - to push the code to git hub.
git push -u origin main - to push (insted of using everytime orgin main we can use only git push).
settings-> pages -> main

repo url is the repositories url and host url is the url of the host.


git clone "repo url" 


